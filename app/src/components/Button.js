import React from "react";

const Button = () => {
  return (
    <div className="buttonContainer">
      <button className="button">Personnaliser ma voiture</button>
    </div>
  );
};

export default Button;
